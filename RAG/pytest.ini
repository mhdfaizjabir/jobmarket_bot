[pytest]
testpaths = tests
markers =
    live: requires the API server running on localhost:8000 (skipped automatically if unreachable)
    llm: makes a real LLM call — costs money/time; only runs when RUN_LLM_TESTS=1
addopts = -q
