# Secrets — never commit
.env
.streamlit/secrets.toml

# ChromaDB index — tracked via Git LFS (*.sqlite3 and *.bin are LFS-tracked)
# chroma_db/ is NOT ignored — pre-built index committed to repo via LFS

# Excel lock files (open-in-Excel temp files)
~$*.xlsx
~$*.xls

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS
.DS_Store
Thumbs.db
