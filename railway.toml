[build]
builder = "nixpacks"
buildCommand = "cd RAG && pip install -r requirements.txt"

[deploy]
startCommand = "cd RAG && uvicorn server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "on-failure"
